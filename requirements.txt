# Core
numpy>=1.26,<3.0
scipy>=1.11
pandas>=2.2.0
polars>=1.4.0
pyarrow>=15.0.0

# Deep learning. The unpinned requirement here resolves to the CPU build by
# default; install the CUDA 12.x build separately if you have an NVIDIA GPU
# (see README — the streaming trainer benefits substantially from GPU bf16).
torch>=2.2
scikit-learn>=1.4.0

# Reinforcement learning environment
gymnasium>=0.29

# Exchange / WebSockets
# US equities (Alpaca) — primary path for the equities pivot. Provides the
# StockDataStream live feed, StockHistoricalDataClient for the historical
# harvester, and TradingClient for /v2/calendar plus order routing.
alpaca-py>=0.13
# Crypto path — kept optional for back-compat / regression on the original
# Coinbase Advanced + Binance configurations.
ccxt>=4.3                 # ccxt.pro is bundled in recent versions; coinbaseadvanced supported
websockets>=12.0          # raw connection used by the (Binance-only) forceOrder liquidation stream

# Hyperliquid historical harvester (fetch_history_hyperliquid.py).
# Both buckets are Requester Pays — boto3 must include RequestPayer="requester"
# on every call. lz4 decompresses the JSONL files in the archive.
boto3>=1.34
lz4>=4.3

# Hyperparameter optimization
optuna>=3.5

# Testing
pytest>=8.0

# Dashboard server (dashboard.py serves the live engine UI over HTTP).
aiohttp>=3.9

# Monitoring & Visualization
tqdm>=4.66.0
matplotlib>=3.8.0
seaborn>=0.13.0
tensorboard>=2.15.0